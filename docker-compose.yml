version: "3.9"

services:
  expense-service:
    build: ./backend/services/expense-service
    ports:
      - "8001:8000"
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"